#include "../main.h"
#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
#include "../main.h"

/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
    int len;
	len = _printf("%b",4294967295);
	printf("len %d\n", len);
    return (0);
}
